# Variables de entorno para funcionalidades dinámicas (futuro)
# El sitio actual es estático (Astro) y NO necesita estas variables para el build.
# Se usarán cuando se integren formularios de inscripción y panel admin (Supabase).

VITE_SUPABASE_URL=your-supabase-project-url
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key